# Cycle 待解决问题

本文跟踪 Cycle 作为“长期驻场负责人”时识别的问题及当前实施状态。Cycle 绑定单个 session，运行状态仅存在于会话与进程内存中；不通过项目文件保存运行状态，也不承诺进程重启后的恢复。

## 1. 文件修改被直接视为进展

**当前状态：按负责人模型重新定为预期行为。任意工具调用都算调度层进展，包括读取、搜索、写入、失败调用和未来新增工具；不再要求 `CYCLE_PROGRESS`。**

此前试图区分活动、持久证据和责任域成果，并为验证命令、VCS、诊断和 todo 增加专门识别。这使引擎需要理解工具语义，也要求模型每轮额外填写结构化声明。

Cycle 的负责人模型采用更简单的边界：工具调用表示模型获得了信息或对责任域采取了行动，因此足以清空 dry 计数。它不是“任务完成”或“行为已验证”的证明；证据等级仍由模型在正常报告中诚实表达。

只有正常结束且完全没有工具调用的 round 才算 dry。失败、中断、空响应和机械重复继续走各自独立的保护逻辑。这样新增任何工具都天然生效，Cycle 引擎不再维护工具名或 Shell 命令分类器。

## 2. Reflect 发现问题后缺少价值排序

**当前状态：已解决提示词语义。Reflect 会先把问题变成候选，再与 backlog 按用户影响、风险、阻塞价值、成本、证据和授权比较。**

当前 Reflect 提示要求任何经审查仍成立的问题都转化为任务并立即行动。这符合负责人的主动性，但没有要求它先与既有 backlog 比较价值、风险、成本和阻塞作用。

结果可能是模型按照发现顺序工作，而不是选择责任域内当前最有价值的事项。新发现的问题不应天然获得最高优先级；它应先成为候选，再与现有工作共同排序。

## 3. 任务阻塞与责任域阻塞没有明确区分

**当前状态：已解决提示词语义。单项 blocked 不阻止 Cycle 选择其他工作；整个责任域都在等待时，模型用正常报告说明条件并结束 round，不输出结构化 outcome。**

一个工作项依赖用户、设备、授权或环境时，只代表该任务被阻塞。Cycle 作为负责人仍应选择责任域内其他可推进的高价值工作。

只有经过 Check、Reflect 和 Plan 后，整个责任域都没有值得独立推进的候选工作时，才应把责任域描述为正在等待外部条件。

当前提示词和状态模型没有充分表达这两个层次，容易把“等待用户完成某项验收”与“整个 Cycle 应暂停”混为一谈。

## 4. todo 无法准确表达阻塞状态

**当前状态：不再作为 Cycle 调度问题。引擎不要求也不解释 todo；模型可以按需使用普通 session todo。仅在 Cycle compaction 清除全部原始对话尾部后，引擎重新注入数据库中的完整 todo 快照。现有字段表达能力的限制仍存在，但不控制调度。**

当前 todo 只记录内容、状态和优先级，无法表达：

- 被什么阻塞；
- 阻塞解除条件；
- 阻塞期间是否应跳过；
- 它是主线、普通候选还是等待事项；
- 是否存在可由 Cycle 自主执行的替代验证路径。

这会造成 todo 与最新证据不一致。例如某项 Demo/native 验证已经完成，真实剩余工作只是等待用户真机验收，但 todo 仍保留旧的“待做验证”描述。

## 5. `blocked` outcome 存在逻辑死角

**当前状态：通过删除 outcome 协议解决。引擎不再解析模型 outcome，也不根据 todo 状态控制调度。**

旧实现只要发现任何未完成 todo，就会忽略模型输出的结构化 outcome。

这可以防止 Cycle 在仍有可执行任务时过早停止，但也意味着：即使所有剩余 todo 都明确依赖外部条件，Cycle 仍无法表达“整个责任域当前已阻塞”，除非先将真实存在的阻塞事项错误地标记为 completed 或 cancelled。

最终没有继续扩展 gate，而是删除了整个模型 outcome 协议。todo 只作为模型的工作上下文；tool-free、重复响应和失败等客观信号由调度器独立处理。

## 6. 上轮摘要截取末尾 300 字符，信息不可靠

**当前状态：已解决。固定尾部摘要已移除；普通 round 直接依赖 session 历史，不生成额外交接。现有模型混沌自评方案也已移除。**

旧实现的 `lastRoundResult` 只保存上一轮自然语言响应的末尾 300 个字符，会：

- 从半句话开始；
- 丢失目标、选择理由和关键证据；
- 只保留结尾的后续动作，缺少其成立条件；
- 让下一轮重复调查或误解工作状态；
- 无法稳定携带责任域、phase、当前任务、阻塞和候选项。

需要在 session/内存中使用比任意尾部截断更可靠的 round handoff。该 handoff 不写入项目目录，也不用于进程重启恢复。

## 7. 轮末缺少 todo 与报告的一致性检查

**当前状态：按简化设计关闭。Cycle 不要求轮末 reconcile；todo 是模型可选的普通工具，其一致性不由调度器检查。普通 round 不注入 todo，只有压缩后的第一次继续会恢复数据库快照。**

模型可能在报告中声称某项验证已完成、某项工作正在等待用户，或发现了新的候选事项，但没有同步更新 todo。

旧设计会把过时的 unfinished-todos 再次注入下一轮，并据此影响模型。该注入已删除；模型如主动使用 todo，仍应自行保持以下内容一致：

- 已完成事项的状态；
- 被阻塞事项及解除条件；
- 新发现的候选；
- 已拒绝或不再有价值的候选；
- 当前选中的下一项工作。

## 8. 缺少本轮意图与完成证据的明确表达

**当前状态：按简化设计关闭。没有新增轮前 intent 或轮末结构化进展协议；目标、结果和证据继续通过正常会话表达，引擎只观察是否调用了工具。**

引擎现在只在轮后观察是否出现工具调用，不尝试知道：

- 本轮选择了什么目标；
- 为什么现在选择它；
- 预期取得什么证据；
- 实际结果是否满足完成条件。

这使“写了一个测试文件”和“建立了经过执行的有效回归保护”无法由引擎自动区分。该区别留给作为负责人的模型判断，避免让 Cycle 引擎成为工具语义分类器，也避免强制模型每轮填写额外协议。

## 9. 缺少工作组合级别的候选比较

**当前状态：按轻量设计处理。反思中的候选比较结果留在正常 session 历史中；引擎只在 Cycle compaction 后恢复已有 todo，不新增持久化或结构化 portfolio。**

Cycle 应把责任域内工作当作一个工作组合，而不是按单一任务链或发现顺序推进。当前状态无法清晰比较：

- 用户主线；
- 被阻塞的验收；
- 未验证修复；
- 测试覆盖缺口；
- 新发现缺陷；
- 文档、提交组织和一般技术债。

候选需要按用户影响、风险、阻塞价值、成本、证据和授权边界进行比较。否则负责人模式可能退化为“看见什么清理什么”。

## 10. 缺少防止“自创任务—完成任务”的约束

**当前状态：明确为模型责任。新候选必须参与价值排序；引擎不判断任务业务价值，任何工具调用仍会清空 dry。机械重复由 doom-loop 等运行保护处理。**

Cycle 有权主动发现并创建任务，但不能通过不断创建低价值小任务来证明自己始终有进展：

```text
没有明显工作
→ 创建一个很小的清理任务
→ 修改文件
→ progress 清空 dry
→ 重复
```

这个模式按当前设计会清空 dry，因为引擎无法通用地区分低价值活动和有效调查。自主创建的任务仍需满足责任域、价值、优先级和验证证据要求，由负责人模型和提示词约束，而不是通过工具分类器实现。

## 11. 单轮可能无限膨胀

**当前状态：重新定性为模型负责的正常执行，不设置 provider-turn 或 round token 上限。引擎只通过 doom-loop、stream stall、unattended 工具超时、连续失败和用户中断处理异常执行，不在正常工作过程中按 provider turn 强制切断。**

真实 session 中已经出现单轮包含 85 个 assistant provider turn、持续约一小时的情况。即使每一步都有意义，过长 round 仍会：

- 使 idle-anchored 调度间隔失去节奏作用；
- 快速膨胀上下文；
- 增加重复操作和判断漂移；
- 延迟用户观察和重新定向的自然边界；
- 让一个工作项长期占据整个工作组合。

提示词继续鼓励“一轮一个可验证单元”，但结束当前工作单元的判断由模型负责。不能因此引入项目文件状态、按 provider turn 干预模型执行，或增加未经设计的重启恢复。

## 12. `exhausted` 缺少价值阈值

**当前状态：已解决提示词语义。Plan 明确允许因价值、确定性、授权或收益成本比不足而拒绝候选，不再要求为了活跃而制造工作。**

足够大的项目永远能找到可修改之处。如果 Plan 只问“还有没有候选”，Cycle 理论上永远不会 exhausted，并可能逐渐退化为低价值代码清理器。

`exhausted` 不应表示项目完美或没有任何可修改内容，而应表示：经过真实候选评估后，剩余事项的价值、确定性、授权或收益成本比不足以支撑下一轮工作。

Cycle 需要能够诚实拒绝候选并记录理由，而不是为了保持运行而制造工作。

## 13. 混沌指数尚未驱动预警与历史重置

**当前状态：现有方案已移除。Cycle 不再要求模型计算混沌指数；新的会话健康方案由后续独立设计替代。**

后续需要在真实 session 校准评分后再启用：首次超过阈值要求 handoff，预警后逐轮评估，下降到解除阈值则恢复；下一轮仍超过触发阈值时才在轮末重置历史。

## 14. 混沌评分可能缺失或格式错误

**当前状态：随模型混沌自评协议删除而消失。**

## 15. 风险信号存在一个 round 的观测延迟

**当前状态：留给新的会话健康方案处理；当前 Cycle 不做逐轮混沌评估或自动历史重置。**

## 16. 混沌评分未进入公开 LoopEvent

**当前状态：随混沌评分状态删除而消失；公开事件无需承载该字段。**

## 17. 后台子进程可绕过 shell timeout

**当前状态：已解决。shell timeout/abort 在终止命令后会主动中断输出读取 fiber，避免 detached descendant 持有 pipe 导致 scope 永久无法关闭；processor 同时对所有 unattended tool 设置总执行时限。**

## 18. 用户 intervention 可能被记为空响应

**当前状态：已解决。Cycle worker 会检查本轮 prompt 之后实际 admitted 的用户消息和 aborted assistant；用户 steer 会暂停 Cycle、让用户 drain 优先，不增加 empty/failure 计数。**

## 19. blocked 二次审计会降低授权边界

**当前状态：随模型 outcome 和二次确认协议删除而消失；一般授权边界仍由 Cycle skill 保留。**

## 20. 模型混沌自评会美化恢复事故

**当前状态：通过删除模型混沌自评解决。新的方案不应再次依赖模型给自身会话状态打分。**

## 约束

上述问题的解决方案必须遵守以下边界：

- 一个 Cycle 只绑定一个 session；
- 调度状态保持 process-local 和 in-memory；
- 不创建 `.opencode/cycle/` 或其他项目内运行状态目录；
- 不将 Cycle 自身的记账、handoff 或 checkpoint 写入项目工作区；
- 不承诺进程退出、服务重启或 session 结束后的恢复；
- 项目文件修改只服务于实际项目成果；
- 在 clustering 或 durable recovery 获得独立设计前，不引入文件式恢复协议。

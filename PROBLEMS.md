# Cycle 待解决问题

本文跟踪 Cycle 作为“长期驻场负责人”时识别的问题及当前实施状态。Cycle 绑定单个 session，运行状态仅存在于会话与进程内存中；不通过项目文件保存运行状态，也不承诺进程重启后的恢复。

## 1. 文件修改被直接视为进展

**当前状态：已解决。文件写入本身不再算进展；`CYCLE_PROGRESS` 必须与新验证、VCS 变更、新的工具诊断证据或真实 todo 状态变化匹配。**

当前引擎将成功的 `edit`、`write`、`apply_patch` 调用直接视为 round progress，并清空 dry 计数。

文件修改只能证明发生了活动，不能证明产生了有效成果。例如：

- 测试文件已经写入，但从未执行；
- 修改没有对应验证；
- 新增的是低价值或无关工作；
- Cycle 自己创建一个小任务，再通过修改文件完成它；
- 修改没有关闭工作项、降低风险或推动已选择的目标。

需要区分活动、持久进展和责任域进展。进展判断应结合本轮目标、工具事实、验证证据、VCS 结果和 todo 状态变化，而不应只看是否发生写入。

## 2. Reflect 发现问题后缺少价值排序

**当前状态：已解决提示词语义。Reflect 会先把问题变成候选，再与 backlog 按用户影响、风险、阻塞价值、成本、证据和授权比较。**

当前 Reflect 提示要求任何经审查仍成立的问题都转化为任务并立即行动。这符合负责人的主动性，但没有要求它先与既有 backlog 比较价值、风险、成本和阻塞作用。

结果可能是模型按照发现顺序工作，而不是选择责任域内当前最有价值的事项。新发现的问题不应天然获得最高优先级；它应先成为候选，再与现有工作共同排序。

## 3. 任务阻塞与责任域阻塞没有明确区分

**当前状态：已解决提示词和 outcome 语义。单项 blocked 不阻止 Cycle 选择其他工作；只有整个责任域无值得独立推进的工作时才报告 blocked。**

一个工作项依赖用户、设备、授权或环境时，只代表该任务被阻塞。Cycle 作为负责人仍应选择责任域内其他可推进的高价值工作。

只有经过 Check、Reflect 和 Plan 后，整个责任域都没有值得独立推进的候选工作时，才应报告 `CYCLE_OUTCOME: blocked`。

当前提示词和状态模型没有充分表达这两个层次，容易把“等待用户完成某项验收”与“整个 Cycle 应暂停”混为一谈。

## 4. todo 无法准确表达阻塞状态

**当前状态：部分解决。`blocked` 已成为 Cycle 识别的正式状态，round prompt 会单列 blocked 工作；解除条件暂时按约定记录在 content 中，尚未增加独立字段。**

当前 todo 只记录内容、状态和优先级，无法表达：

- 被什么阻塞；
- 阻塞解除条件；
- 阻塞期间是否应跳过；
- 它是主线、普通候选还是等待事项；
- 是否存在可由 Cycle 自主执行的替代验证路径。

这会造成 todo 与最新证据不一致。例如某项 Demo/native 验证已经完成，真实剩余工作只是等待用户真机验收，但 todo 仍保留旧的“待做验证”描述。

## 5. `blocked` outcome 存在逻辑死角

**当前状态：已解决。只有 pending/in_progress 等 actionable todo 会 gate outcome；blocked todo 保留但不再屏蔽责任域级 outcome。**

当前引擎只要发现任何未完成 todo，就完全忽略模型输出的 `CYCLE_OUTCOME: exhausted|blocked`。

这可以防止 Cycle 在仍有可执行任务时过早停止，但也意味着：即使所有剩余 todo 都明确依赖外部条件，Cycle 仍无法表达“整个责任域当前已阻塞”，除非先将真实存在的阻塞事项错误地标记为 completed 或 cancelled。

outcome gate 应基于“是否仍有可执行且值得执行的工作”，而不只是“是否存在任何未完成 todo”。

## 6. 上轮摘要截取末尾 300 字符，信息不可靠

**当前状态：已解决。固定尾部摘要已移除。混沌指数观测已加入，但自动 handoff 与历史重置属于后续阶段。**

旧实现的 `lastRoundResult` 只保存上一轮自然语言响应的末尾 300 个字符，会：

- 从半句话开始；
- 丢失目标、选择理由和关键证据；
- 只保留结尾的后续动作，缺少其成立条件；
- 让下一轮重复调查或误解工作状态；
- 无法稳定携带责任域、phase、当前任务、阻塞和候选项。

需要在 session/内存中使用比任意尾部截断更可靠的 round handoff。该 handoff 不写入项目目录，也不用于进程重启恢复。

## 7. 轮末缺少 todo 与报告的一致性检查

**当前状态：部分解决。每轮 prompt 强制在报告前 reconcile todo；todo 类进展只有在本轮确实调用 `todowrite` 且列表实际变化时才成立。引擎尚不能语义验证报告中的每个完成声明是否与 todo 一致。**

模型可能在报告中声称某项验证已完成、某项工作正在等待用户，或发现了新的候选事项，但没有同步更新 todo。

下一轮随后会收到过时的 unfinished-todos 提示，并依据已经失真的 backlog 工作。Cycle 长期运行时需要保持以下内容一致：

- 已完成事项的状态；
- 被阻塞事项及解除条件；
- 新发现的候选；
- 已拒绝或不再有价值的候选；
- 当前选中的下一项工作。

## 8. 缺少本轮意图与完成证据的明确表达

**当前状态：基本解决。每轮必须输出结构化 `CYCLE_PROGRESS` 的 kind、goal 和 evidence，并与工具事实交叉检查；当前目标仍在轮末声明，尚未建立轮前 intent 协议。**

引擎目前主要在轮后根据工具类型猜测是否发生了进展，但不知道：

- 本轮选择了什么目标；
- 为什么现在选择它；
- 预期取得什么证据；
- 实际结果是否满足完成条件。

这使“写了一个测试文件”和“建立了经过执行的有效回归保护”难以区分。需要在 session 内形成简短、稳定的本轮意图、结果和证据表达，并与可观察的工具事实交叉检查。

## 9. 缺少工作组合级别的候选比较

**当前状态：部分解决。Reflect/Plan 已要求统一候选排序，blocked 与 actionable 工作会分组注入 prompt；候选比较仍存在于 session 自然语言和 todo 中，没有新增持久化或结构化 portfolio。**

Cycle 应把责任域内工作当作一个工作组合，而不是按单一任务链或发现顺序推进。当前状态无法清晰比较：

- 用户主线；
- 被阻塞的验收；
- 未验证修复；
- 测试覆盖缺口；
- 新发现缺陷；
- 文档、提交组织和一般技术债。

候选需要按用户影响、风险、阻塞价值、成本、证据和授权边界进行比较。否则负责人模式可能退化为“看见什么清理什么”。

## 10. 缺少防止“自创任务—完成任务”的约束

**当前状态：已显著缓解。新候选必须参与价值排序，文件写入不算进展，诊断证据不能重复，todo 必须真实变化；引擎仍无法完全判断一个新任务的业务价值。**

Cycle 有权主动发现并创建任务，但不能通过不断创建低价值小任务来证明自己始终有进展：

```text
没有明显工作
→ 创建一个很小的清理任务
→ 修改文件
→ progress 清空 dry
→ 重复
```

自主创建的任务仍需满足责任域、价值、优先级和验证证据要求。目标是防止制造活动，而不是限制负责人发现真实工作。

## 11. 单轮可能无限膨胀

**当前状态：已解决 provider-turn 失控。达到 `loopConfig.maxRoundProviderTurns` 时注入收尾提示，下一 turn 禁用工具并强制结束本轮；在途工具不被轮次边界打断。另有 unattended 工具总时限作为兜底。尚未增加 round token 上限。**

真实 session 中已经出现单轮包含 85 个 assistant provider turn、持续约一小时的情况。即使每一步都有意义，过长 round 仍会：

- 使 idle-anchored 调度间隔失去节奏作用；
- 快速膨胀上下文；
- 增加重复操作和判断漂移；
- 延迟用户观察和重新定向的自然边界；
- 让一个工作项长期占据整个工作组合。

需要鼓励或约束“一轮一个可验证单元”，并在安全边界形成仅存在于当前 session 的 checkpoint。不能因此引入项目文件状态或未经设计的重启恢复。

## 12. `exhausted` 缺少价值阈值

**当前状态：已解决提示词语义。Plan 明确允许因价值、确定性、授权或收益成本比不足而拒绝候选，不再要求为了活跃而制造工作。**

足够大的项目永远能找到可修改之处。如果 Plan 只问“还有没有候选”，Cycle 理论上永远不会 exhausted，并可能逐渐退化为低价值代码清理器。

`exhausted` 不应表示项目完美或没有任何可修改内容，而应表示：经过真实候选评估后，剩余事项的价值、确定性、授权或收益成本比不足以支撑下一轮工作。

Cycle 需要能够诚实拒绝候选并记录理由，而不是为了保持运行而制造工作。

## 13. 混沌指数尚未驱动预警与历史重置

**当前状态：按阶段设计暂未实施。当前只采样、计算、记录并通过 `/cycle status` 展示混沌指数。**

后续需要在真实 session 校准评分后再启用：首次超过阈值要求 handoff，预警后逐轮评估，下降到解除阈值则恢复；下一轮仍超过触发阈值时才在轮末重置历史。

## 14. 混沌评分可能缺失或格式错误

**当前状态：部分解决。被要求评估却没有返回合法 `CYCLE_CHAOS` 时会写 warning 日志，并保留上次有效分数；尚未在 `/cycle status` 中展示评估失败或分数新鲜度。**

## 15. 风险信号存在一个 round 的观测延迟

**当前状态：已知设计边界。no-progress、失败、重复响应等本轮结果只能触发下一轮评估；上下文利用率可在本轮 prompt 构造前触发。启用自动重置前需要保留这一时序语义。**

## 16. 混沌评分未进入公开 LoopEvent

**当前状态：观测阶段有意保持内部。评分存在于进程内 `LoopState`、日志和 `/cycle status`，TUI/footer 与外部客户端暂不可见。若真实数据校准需要跨进程采集，应单独设计公开事件字段。**

## 17. 后台子进程可绕过 shell timeout

**当前状态：已解决。shell timeout/abort 在终止命令后会主动中断输出读取 fiber，避免 detached descendant 持有 pipe 导致 scope 永久无法关闭；processor 同时对所有 unattended tool 设置总执行时限。**

## 18. 用户 intervention 可能被记为空响应

**当前状态：已解决。Cycle worker 会检查本轮 prompt 之后实际 admitted 的用户消息和 aborted assistant；用户 steer 会暂停 Cycle、让用户 drain 优先，不增加 empty/failure 计数。**

## 19. blocked 二次审计会降低授权边界

**当前状态：已解决提示词约束。第二次确认仅允许低成本只读复核；没有新证据或外部条件变化时保留 unblock condition，不得把用户物理操作、账号访问或外部 UI 控制重新解释成自主授权。**

## 20. 模型混沌自评会美化恢复事故

**当前状态：已缓解。引擎现在按单轮 provider turns、工具数/错误、时长、上下文利用率和破坏性操作校准各维度下限；模型仍负责解释原因。阈值和权重仍需更多真实 session 校准。**

## 约束

上述问题的解决方案必须遵守以下边界：

- 一个 Cycle 只绑定一个 session；
- 调度状态保持 process-local 和 in-memory；
- 不创建 `.opencode/cycle/` 或其他项目内运行状态目录；
- 不将 Cycle 自身的记账、handoff 或 checkpoint 写入项目工作区；
- 不承诺进程退出、服务重启或 session 结束后的恢复；
- 项目文件修改只服务于实际项目成果；
- 在 clustering 或 durable recovery 获得独立设计前，不引入文件式恢复协议。
